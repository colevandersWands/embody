# Depth Tracker

> is this a separate module? or integrated with an environment module?

Tracks current depth of scopes (function calls & blocks), _and_ parentheses. This should store enough data (when fully configured) to replicate what's shown in the devtools + parentheses within the current scope.

/**
 * @file Default configuration for educational execution tracer
 *
 * Provides the complete default configuration values for the educational
 * execution tracer. This configuration represents a balanced 'detailed'
 * preset suitable for most educational scenarios.
 *
 * @see README.md for comprehensive documentation
 */

import { Config } from './types.js';

const defaultConfig: Config = {
  // === PRESET SYSTEM ===
  // === LANGUAGE FEATURES ===
};

export default defaultConfig;
